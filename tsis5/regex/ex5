import re

def match_a_followed_by_anything_ending_in_b():
    with open('row.txt', 'r') as file:
        data = file.read()
        pattern = re.compile(r'a.*b')
        matches = pattern.findall(data)
        return matches
    
print("Task 5:", match_a_followed_by_anything_ending_in_b())